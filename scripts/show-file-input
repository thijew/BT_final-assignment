// validate file input

document.querySelector('#docPicker').addEventListener('change', (e) => {
    const fileUploaded = e.target.files.length > 0;
    console.log(fileUploaded ? "File chosen!" : "No file chosen.");
});